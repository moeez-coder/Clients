# Query — Portfolio Without a Throughline, 2026-09-21

**Campaign 7** — segment: identical to campaign 4(a) per the open item recorded in
`campaigns/2026-09-21-medtech-gtm-campaign-set-v2.md` ("multi-BU device/diagnostics, $250M-$3B,
3+ brands into overlapping call points") — same universe, different persona (VP Product
Marketing/CMO here vs. the newest senior commercial hire in campaign 4).

**No new API call was made for this segment.** The 98 rows in this CSV are the same 98 companies
sourced for `new-leader-old-portfolio` on the same date (see that folder's `.query.md` for the actual
filter, provider, and cost) — copied here rather than re-queried, since re-running an identical
firmographic filter against the same provider would have produced the same company set at needless
extra credit cost with the budget nearly exhausted. Each row is tagged `_reused_from` in the raw JSON
(not carried into the CSV) recording this. Decide in a future session whether these two campaigns
should in fact share one sourcing config going forward instead of two folders with duplicate CSVs —
this pull defers that decision, it doesn't resolve it.

**Cost:** $0 (reused, no new call).
